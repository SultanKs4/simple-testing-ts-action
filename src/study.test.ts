import { expect } from "chai";

describe("study testing", () => {
  it("Testing jest", () => {
    expect(2).equal(2);
  });

  it("Can more than testing in 1 file", () => {
    expect(6 + 4).equal(10);
  });
});
